on:
  workflow_dispatch:
  schedule:
    - cron: '45 2,5,8,11 * * *'


jobs:

  cron:
    runs-on: ubuntu-latest

    steps:
      - uses: li1ht2ay/it1h-c3@cron
        with:
          token: ${{ secrets.token }}
